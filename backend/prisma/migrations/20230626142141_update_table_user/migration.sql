BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[t_usuario] ADD [ativo] BIT NOT NULL CONSTRAINT [t_usuario_ativo_df] DEFAULT 1,
[ultimo_login] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
